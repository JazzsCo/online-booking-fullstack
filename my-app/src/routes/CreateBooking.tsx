import LinearStepper from "../components/Stepper/LinearStepper";

const CreateBooking = () => {
  return (
    <div
      style={{
        textAlign: "center",
        maxWidth: "800px",
        margin: "0 auto",
        marginTop: "100px",
      }}
    >
      <LinearStepper />
    </div>
  );
};

export default CreateBooking;
